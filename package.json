{
  "name": "nonprofit-pickup-tracker",
  "version": "0.1.0",
  "private": true,
  "main": "app/main.js",
  "scripts": {
    "start": "node server/index.js",
    "start:electron": "electron .",
    "install:all": "cd server && npm install && cd ../app && npm install"
  }
}
